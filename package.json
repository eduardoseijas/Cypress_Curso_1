{
  "name": "practica_cypress_1",
  "version": "1.0.0",
  "description": "Curso_Cypress_2025",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ci": "cypress run --browser chrome --headless",
    "ci:spec": "cypress run --browser chrome --headless --spec"
  },
  "author": "Eduardo Seijas",
  "license": "ISC",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.3.0",
    "@badeball/cypress-cucumber-preprocessor": "^23.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "@types/cypress": "^0.1.6",
    "cypress": "^15.0.0",
    "cypress-file-upload": "^5.0.8",
    "cypress-plugin-tab": "^1.0.5",
    "cypress-real-events": "^1.14.0",
    "cypress-xpath": "^2.0.1",
    "esbuild": "^0.25.9",
    "lambdatest-cypress-cli": "^3.0.42"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/step_definitions/**/*.js"
    ]
  }
}
